import 'package:flutter/material.dart';

// ignore_for_file: use_key_in_widget_constructors

class MyText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text(
      "Welcome to Food Genes & Me",
      style: TextStyle(
        color: Colors.white,
        fontSize: 32.0,
      ),
    );
  }
}
